# US1 Capture Flow

## 目标

验证快速记录窗口可以真实保存任务，并在应用重启后重新读取。

## 步骤

1. 启动应用，确认默认显示“快速记录窗口”。
2. 输入一条任务并点击“保存任务”。
3. 确认页面出现“保存成功”，并在“今日已保存任务”区域看到该任务。
4. 再连续输入两条不同任务并保存。
5. 确认列表中累计出现三条任务。
6. 关闭应用后重新启动。
7. 再次进入“快速记录窗口”，确认三条任务仍能从本地 SQLite 读出。
