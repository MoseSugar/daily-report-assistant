{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the app skeleton",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
